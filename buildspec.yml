version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14.x
    commands:
      - npm install
      - npm update
  build:
    commands:
      - npm run build

artifacts:
  base-directory: ./build
  files:
    - '**/*'
  
